{
    "configurations": [
        {
            "name": "Python Debugger: Module",
            "type": "debugpy",
            "request": "launch",
            "module": "server",
            "args": ["--reload"],
            "cwd": "${workspaceFolder}/src"
        }
    ]
}
